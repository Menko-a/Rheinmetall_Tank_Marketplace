const config = {
	input: "./src/globals.css",
	// NativeWind supports RN-web; we include common entry points.
	// If you later add/adjust structure, update the paths here.
	output: "./.nativewind",
};

export default config;
